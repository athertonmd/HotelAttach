{
  "name": "@hci/behaviour-intelligence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Behaviour Intelligence Engine — domain model and logic",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src/ --ignore-pattern 'src/__tests__/'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hci/event-contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
