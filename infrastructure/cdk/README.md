# CDK Infrastructure Stacks

AWS CDK stacks for the HCI platform. Each subdirectory contains a focused stack.

Implementation will follow the MVP build sequence — infrastructure is provisioned incrementally as services are built.
