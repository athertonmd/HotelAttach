/**
 * @hci/shared-types
 *
 * Shared TypeScript types for the Hotel Compliance Intelligence platform.
 *
 * This package contains:
 * - Domain entity interfaces (Traveller, Trip, PNR, Segment, etc.)
 * - Enums (TripStatus, SegmentType, OpportunityType, etc.)
 * - Value objects (TenantId, CorporateId, CorrelationId, etc.)
 * - Common utility types used across all services
 *
 * All types defined here are derived from the governance documentation
 * and JSON event schemas in /docs and /schemas.
 */

export {};
